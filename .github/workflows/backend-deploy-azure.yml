name: Deploy Backend (Azure)

on:
  push:
    branches: ["**"]
    paths:
      - "backend/**"

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    # main ブランチ → azure-prod 環境、それ以外 → azure-dev 環境
    # 各 GitHub Environment に環境固有のシークレットを設定しておく
    environment: ${{ github.ref_name == 'main' && 'azure-prod' || 'azure-dev' }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies into package directory
        run: |
          pip install -r requirements-azure.txt -t ./package
          cp -r app ./package/
          cp function_app.py ./package/
          cp host.json ./package/
        working-directory: backend

      - name: Create deployment ZIP
        # function_app.py と host.json が ZIP ルートに来るようにする
        run: cd package && zip -r ../api-azure.zip .
        working-directory: backend

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy to Azure Functions
        run: |
          az functionapp deployment source config-zip \
            --name ${{ secrets.AZURE_FUNCTIONAPP_NAME }} \
            --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
            --src backend/api-azure.zip
